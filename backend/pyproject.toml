[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"

[tool.coverage.run]
source = [
    "routes",
]
omit = [
    "routes/tutor_brain.py",
]

[tool.coverage.report]
show_missing = true
fail_under = 80
